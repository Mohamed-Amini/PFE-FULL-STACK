
  <div class="container-fluid bg-light py-5">
    <div class="row justify-content-center align-items-center">
      <div class="col-md-6">
        <h1 class="text-center">Hi! This is Kinesis Bot</h1>
        <p class="text-center">To reset your password, please visit the following link</p>
        <p class="text-center">Cheers!</p>
      </div>
    </div>
  </div>

  <div class="app font-sans min-w-screen min-h-screen bg-grey-lighter py-8 px-4">
    <div class="mailwrapper max-w-md mx-auto">
      <div class="mailcontent bg-white p-8 shadow-md">
        <div class="contentheader text-center tracking-wide border-bottom pb-3">
          <div class="text-blue text-xl font-bold">Kinesis</div>
          <h1 class="text-3xl h-48 flex items-center justify-center">E-mail Confirmation</h1>
        </div>
        <div class="contentbody py-8 border-bottom">
          <p>Hey,<br><br>It looks like you just signed up for The App, that’s awesome! Can we ask you for email confirmation? Just click the button below.</p>
          <a class="btn btn-primary btn-lg btn-block mt-4" href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a>
          <p class="text-sm mt-4">Keep Rockin'!<br> Your The App team</p>
        </div>
        <p class="text-sm">This link will expire in {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>
        <div class="contentfooter mt-8 text-center text-grey-darker">
          <h3 class="text-base sm:text-lg mb-4">Thanks for using The Kinesis!</h3>
          <p>www.kinesis.com</p>
        </div>
      </div>
      <div class="mailmeta text-center text-sm text-grey-darker mt-8">
        <div class="meta__social flex justify-center my-4"></div>
      </div>
    </div>
  </div>
